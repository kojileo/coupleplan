{
  "buildCommand": "npm run vercel-build",
  "installCommand": "rm -rf node_modules/.prisma && npm install && npm run postinstall",
  "framework": "nextjs",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ],
  "env": {
    "PRISMA_CLIENT_ENGINE_TYPE": "library"
  }
}
